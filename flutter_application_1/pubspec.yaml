name: flutter_application_1
description: A new Flutter project for Dealora

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # 🔗 Firebase
  firebase_core: ^2.31.0        # Inicialización de Firebase
  firebase_auth: ^4.17.0        # Inicio de sesión / registro
  cloud_firestore: ^4.16.0      # Base de datos en la nube
  google_sign_in: ^6.1.5        # Login rápido con Google

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/

  fonts:
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto.ttf
        - asset: assets/fonts/Roboto-Italic.ttf
          style: italic
